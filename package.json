{
  "name": "mono-file-swagger",
  "version": "2.2.0",
  "description": "Mono-file Swagger",
  "main": "resolve.js",
  "scripts": {
    "start": "node resolve.js",
    "test": "node resolve.js -o yaml openapi.yaml actual && diff -ur expect actual"
  },
  "bin": {
    "multi-file-swagger": "resolve.js"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^2.9.0",
    "js-yaml": "^3.9.1",
    "json-refs": "^2.1.6"
  }
}
